# Data Directory

Future home for static seed data before a database exists.

Do not add generated bulk content yet. The current validated content still lives in `app.js`.

Expected future folders:

- `seed/` - movements, works, paths, awards, lists, authors, publishers, translators
