# Prisma Boundary

`schema.prisma` mirrors the first product slice from the BookPath spec. It is present because the initial product now follows the Next.js + Prisma architecture, but no database is required to inspect the route shell.
