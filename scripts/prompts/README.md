# Prompt Templates

Future prompt builders from the full BookPath specification:

- `movement`
- `work-guide`
- `reading-path`

Current rule: do not add AI API calls in the static MVP. Prompt templates can be drafted later as plain files, but generation must remain outside this prototype until explicitly approved.
